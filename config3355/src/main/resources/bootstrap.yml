server:
  port: 3355
spring:
  application:
    name: config
  cloud:
    compatibility-verifier:
      enabled: false
    config:
      uri:
        http://localhost:3344/
      profile:
        dev
      name:
        config
      label:
        master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/